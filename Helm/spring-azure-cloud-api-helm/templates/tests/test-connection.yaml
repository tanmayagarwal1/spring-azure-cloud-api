apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-azure-cloud-api-helm.fullname" . }}-test-connection"
  labels:
    {{- include "spring-azure-cloud-api-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-azure-cloud-api-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
